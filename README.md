# My useful functions

## What does this repository do?

It contains the shell function that I find useful

## How to make this running

Add `source <repository-directory>/srcAll.sh` to the end of ~/.zshrc | ~/.bashrc ...

Then, restart the terminal or run `source ~/<the-rc-file>`.

